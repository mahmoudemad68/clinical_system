name: clinic_secure_storage
description: Platform key-store backed storage for tokens and the local database key.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: 11.0.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

# Part of the root pub workspace; shares the root lockfile.
resolution: workspace
