/// Arabic and English localization for the clinic clients.
///
/// Every user-visible string lives here. Hard-coded strings in widgets are
/// forbidden (plan.md section 148), because a string in a widget is a string
/// that will never be translated.
library;

export 'src/clinic_strings.dart';
export 'src/locale_support.dart';
