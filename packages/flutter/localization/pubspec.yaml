name: clinic_localization
description: Arabic and English strings and locale handling for the clinic clients.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

# Part of the root pub workspace; shares the root lockfile.
resolution: workspace
