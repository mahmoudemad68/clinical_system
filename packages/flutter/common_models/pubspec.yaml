name: clinic_common_models
description: Shared domain and view models for the clinic Flutter clients.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.13.0

dependencies:
  meta: ^1.16.0

dev_dependencies:
  flutter_lints: ^6.0.0
  test: ^1.26.0

# Part of the root pub workspace; shares the root lockfile.
resolution: workspace
