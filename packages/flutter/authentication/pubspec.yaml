name: clinic_authentication
description: Session, device token, OTP, and MFA client flows.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  clinic_error_handling:
    path: ../error_handling
  clinic_networking:
    path: ../networking
  clinic_secure_storage:
    path: ../secure_storage
  dio: ^5.9.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

resolution: workspace
